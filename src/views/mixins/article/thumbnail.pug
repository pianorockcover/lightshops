mixin articleThumbnail()
    -
        const getRandom = (min, max) => Math.trunc(Math.random() * (max - min) + min);
        const randomElem = (items) =>  items[Math.floor(Math.random() * items.length)];

        const thumbnail = `images/articles/thumbnail-${getRandom(1, 3)}.jpeg`;
        const ava = `images/articles/author-${getRandom(1, 2)}.jpeg`;
        const author = randomElem(["John Doe", "Bob Jones", "Matt Smith"]);
        const title = randomElem(["Goodby Summer, Hello Autumm!", "The Best Camera For Yout Trip"]);
        const text = `Contrary to popular belief, Lorem Ipsum is not simply random text. It has roots in a piece of classical...`

    .article-thumbnail
        .article-thumbnail-image-wrapper
            img.article-thumbnail-image(src=thumbnail alt="Article thumbnail")
            <svg class="article-thumbnail-wave" viewbox="0 0 100 25">
                <path class="article-thumbnail-wave-path" opacity="0.5" d="M0 30 V15 Q30 3 60 15 V30z" />
                <path class="article-thumbnail-wave-path" d="M0 30 V12 Q30 17 55 12 T100 11 V30z" />
            </svg>
        .article-thumbnail-author
            img.article-thumbnail-author-image(src=ava alt="Article author")
            .article-thumbnail-author-name
                b.text-contrast by 
                | #{author}
        .article-thumbnail-title
            | #{title}
        .article-thumbnail-text
            | #{text}
        .article-thumbnail-footer
            .article-thumbnail-date
                span.bi.bi-calendar-event-fill.article-thumbnail-calendar-icon
                |  Aug 12, 2021
            a.btn.article-thumbnail-more.btn-contrast(href="#")
                span.bi.bi-arrow-right
                | Read more


