mixin breadcrumbs(items)
    .breadcrumbs
        - for (let i in items) {
            if (i == items.length - 1)
                .breadcrumbs-current= items[i]
            else
                a.breadcrumbs-link(href="category-1.html")= items[i]
                span.breadcrumbs-divider /
        - }